guard :shell do
  watch(/^Makefile|.*\.(rb|feature|d)$/) do |m|
    title = 'Cucumber'
    eager 'cucumber 2>&1'
    status = ($?.success? && :success) || :failed
    n '', title, status
    ''
  end
end
